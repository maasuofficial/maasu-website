{
  "name": "maasu",
  "version": "0.3.0",
  "private": false,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "react-scripts build && firebase deploy",
    "deploy-dev": "react-scripts build && firebase deploy",
    "deploy-prod": "firebase use prod && react-scripts build && firebase deploy && firebase use dev",
    "test": "react-scripts test --coverage --watchAll",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "querystring": "^0.2.0",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-icons": "^3.8.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.0.1",
    "react-utility-components": "^0.2.3",
    "video-react": "^0.14.1"
  },
  "devDependencies": {
    "@testing-library/react": "^8.0.9",
    "eslint": "^5.16.0",
    "eslint-plugin-react": "^7.16.0",
    "jest-dom": "^3.5.0",
    "node-sass": "^4.13.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
